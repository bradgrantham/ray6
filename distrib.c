
#include "grf_math.h"


/* reflect = reflected (outgoing) ray */
/* normal = normal to reflecting plane */
/* ref_per = percentage reflection; 1.0=complete reflection, 0.0=diffuse */

vec4type *ray_distrib(vec4type reflect[2], vec4type normal[2], double ref_per, 
 vec4type perturb[2])
{
   
   return(perturb);
}
